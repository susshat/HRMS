import { ComponentProps } from 'react';

export type DrawerFooterProps = ComponentProps<'footer'>;

const DrawerFooter = (props: DrawerFooterProps) => <footer {...props}></footer>;

export default DrawerFooter;
